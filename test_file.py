import unittest

from day4 import sum_list, power


class TestSumingList(unittest.TestCase):

    def sum_list(self):
        my_list = [1, 2, 3, [1, 2, 3]]

        self.assertEqual(result, 6)

    def power(self):
        if(y == 1):
            self.assertEqual(result, x*power(x, y-1))

if __name__ == "__main__":
    unittest()
